{
  "name": "harmony-plate-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.0.0"
    // Other dependencies will be added as needed
  },
  "scripts": {
    "start": "echo "Run a development server, e.g., using 'react-scripts start' or Vite"",
    "build": "echo "Build the app for production"",
    "test": "echo "Run tests""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
